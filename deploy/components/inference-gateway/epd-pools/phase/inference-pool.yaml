apiVersion: inference.networking.k8s.io/v1
kind: InferencePool
metadata:
  name: ${POOL_NAME}-${PHASE}
spec:
  selector:
    matchLabels:
      llm-d.ai/role: ${PHASE}
  endpointPickerRef:
    name: ${EPP_NAME}-${PHASE}
    kind: Service
    port:
      number: 9002
  targetPorts:
  - number: 8000
