# ------------------------------------------------------------------------------
# VLLM Prefill Component
#
# Prefill deployment for disaggregation scenarios (P/D, E/P/D).
# Also used by the coordinator (test/e2e/artifacts/environments/dev/coordinator-epd).
# ------------------------------------------------------------------------------
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
